# Front-End

The demo retains the original STS stylesheets, page layouts, theme control, navigation, charts and form interactions.

## Interface

HTML and CSS define the existing screens. JavaScript powers conditional form fields, room checklists, asset search, table filtering, CSV exports and live dashboard updates.

## Static adaptation

A shared JavaScript adapter supplies fictional responses in place of PHP endpoints. Libraries and fonts are served locally.
