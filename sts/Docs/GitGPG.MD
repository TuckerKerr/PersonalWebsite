# Source integrity

This demo is a separate copy of STS. Original backend files and deployment settings are not included.

## Dependencies

Chart.js, Font Awesome, JSConfetti, Marked, Highlight.js and Inter are served locally with their license notices.

## Demo state

Each hosting path uses its own storage namespace so the demo does not overwrite another portfolio application’s session keys.
